# Nome da aplicação
spring.application.name=applications

# Configurações do banco de dados H2
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# Habilita o console web do H2
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console



# Inicialização do banco de dados com scripts (descomente se necessário)
# spring.datasource.initialization-mode=always
# spring.datasource.schema=classpath:schema.sql
# spring.datasource.data=classpath:data.sql

# Especifica o arquivo data.sql a ser usado.
# spring.datasource.data=classpath:data.sql
# Define que o Spring deve sempre executar o SQL de inicialização.
# spring.sql.init.mode=always
# Usado em versões mais antigas do Spring Boot.
# spring.datasource.initialization-mode=always

# local para procurar rederizar as JSPs
#spring.mvc.view.prefix=/WEB-INF/views/
#spring.mvc.view.suffix=.jsp